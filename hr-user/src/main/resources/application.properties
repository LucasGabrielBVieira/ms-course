spring.application.name=hr-user
server.port=${PORT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.application-id:${random.value}}

eureka.client.service-url.defaultZone=${EUREKA_URI:http://hr-eureka-server:8761/eureka}