spring.application.name=hr-oauth
server.port=${PORT:0}

eureka.instance.instance-id=${spring.application.name}${spring.application-id:${random.value}}

eureka.client.service-url.defaultZone=http://hr-eurekaserver:8761/eureka

